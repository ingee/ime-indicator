# CLAUDE.md — Windows IME 상태 인디케이터 (TSF 이벤트 기반)

## 1. 프로젝트 개요

Windows에서 한글/영문 입력 상태(IME)를 화면에 항상 작게 표시해주는 상시 실행 프로그램을 만든다.
사용자는 듀얼 모니터 환경에서 작업하며, 현재 입력 상태(한글/영문)를 매번 인지하지 못해 오타가
반복되는 문제를 겪고 있다. 화면 구석에 상시 표시되는 작은 인디케이터로 이를 해결한다.

## 2. 배경 — 기존 프로토타입(AutoHotkey)에서 확인된 사실

AutoHotkey로 먼저 프로토타입을 만들어 검증했고, 이 과정에서 아래 사실들을 확인했다.
새 구현(C#)은 이 교훈을 반영해야 한다.

- 이 시스템은 모든 애플리케이션에서 레거시 IMM API(`ImmGetContext`)가 `himc=0`을 반환한다.
  즉 이 PC의 입력기는 **TSF(Text Services Framework)로만 동작**하며, 레거시 IMM 호환 계층이
  사실상 작동하지 않는다.
- 차선책으로 `VK_HANGUL`(가상키코드 0x15)의 전역 토글 상태(Caps Lock과 같은 방식)를 읽는
  방식을 시도했으나, 이 값은 TSF가 관리하는 "진짜 상태"의 **메아리(echo)**에 불과했다.
  실제 입력 상태가 바뀌어도 이 토글 값이 갱신되지 않는 경우가 있어, 표시와 실제 상태가
  어긋난 채로 장시간 지속되는 문제가 반복됐다.
- 폴링(주기적 재확인) 방식으로 이 값을 반복 조회하는 것은 근본적인 해결책이 되지 못했다.
  값 자체가 부정확하면 몇 번을 다시 읽어도 같은 부정확한 값만 반환되기 때문이다.
- **결론: 반드시 TSF의 진짜 상태 저장소(Compartment)를 직접 읽어야 하며, 폴링이 아니라
  TSF의 이벤트 콜백(`ITfSource` / `AdviseSink`)을 구독하는 방식으로 구현해야 타이밍 어긋남
  문제가 구조적으로 발생하지 않는다.**

## 3. 목표 / 비목표

**목표**
- 현재 포커스된 컨텍스트의 한/영 입력 상태를 **오차 없이, 실시간으로** 표시
- 폴링 없이 TSF 이벤트 구독만으로 상태 변화 감지
- 여러 모니터 각각에 인디케이터 표시

**비목표**
- IME 자체의 동작을 변경하거나 가로채는 기능 (표시 전용)
- 한글 외 다른 언어(일본어 가나 등) 지원 — 필요 시 추후 확장

## 4. 핵심 기술 요구사항

- 언어/런타임: C# (.NET), Windows 전용
- TSF COM 인터페이스를 직접 P/Invoke 또는 COM 상호운용으로 사용:
  - `ITfThreadMgr` (스레드 매니저 생성/초기화)
  - `ITfCompartmentMgr` → `GUID_COMPARTMENT_KEYBOARD_OPENCLOSE` 컴파트먼트로 한/영
    켜짐·꺼짐 상태 조회
  - `ITfSource` + 컴파트먼트 이벤트 싱크(`ITfCompartmentEventSink`)를 구현한 콜백 객체를
    `AdviseSink`로 등록하여, 상태가 바뀌는 시점에 `OnChange` 콜백으로 즉시 통지받는다.
- **폴링 타이머를 사용하지 않는다.** 상태 갱신은 오직 TSF 콜백에 의해서만 트리거된다.
- 프로그램 시작 시 최초 1회는 현재 상태를 조회해서 초기 화면을 맞춘다(이후로는 콜백에만 의존).
- COM 호출 실패, 예외 상황에 대한 방어적 처리 포함 (크래시 없이 인디케이터가 유지되어야 함).

## 5. UI 스펙

- 모니터 수를 자동 감지하여, **각 모니터 상단 중앙**에 인디케이터 창을 하나씩 표시
- 창 크기: 정사각형, 한 변 약 36px (조정 가능하도록 설정값으로 분리)
- 배경색: 완전 불투명
  - 한글 모드: 빨강 (`#FF0000`)
  - 영문 모드: 파랑 (`#0000FF`)
- 텍스트: 흰색, 굵게, 약 18pt 상당 크기, 가운데 정렬
  - 한글 모드: "한"
  - 영문 모드: "A"
- 창 속성: 항상 위(topmost), 포커스를 가져가지 않음(no-activate), 작업표시줄/Alt-Tab
  목록에 표시되지 않음(tool window)
- 리소스 사용량 최소화 (상시 실행 프로그램이므로 CPU/메모리 사용을 낮게 유지)

## 6. 상호작용 (안전장치)

TSF 이벤트 기반이면 이론적으로 어긋남이 발생하지 않아야 하지만, 예외적인 상황(레거시
프로그램, COM 이벤트 유실 등)에 대비한 수동 교정 기능을 남겨둔다.

- 인디케이터를 **클릭**하면 현재 표시를 즉시 반대로 뒤집는다(API 재조회 없이 단순 반전).
- 이 수동 교정 이후에도 실제 TSF 이벤트가 발생하면 정상적으로 새로운 상태를 반영해야 한다.

## 7. 비기능 요구사항

- 시작프로그램에 등록되어 부팅 시 자동 실행
- 사용자는 이미 별도의 AutoHotkey 스크립트(ESC 키 → 영문 강제 전환)를 시작프로그램에서
  함께 실행 중이다. 이 프로그램은 그 스크립트와 **독립적으로 동시에 실행되어도 충돌이
  없어야 한다** (전역 단축키를 새로 점유하지 않는 것을 권장).
- 종료 방법(트레이 아이콘 컨텍스트 메뉴 등)을 제공할 것

## 8. 참고 — 이전 프로토타입 파일

`ime_indicator.ahk` (AutoHotkey, 참고용 — 이번 프로젝트에서는 사용하지 않음): 위 3번
항목에 정리된 한계로 인해 대체되는 것이며, UI 스펙(크기/색상/위치)은 이 파일에서 이미
사용자 검증을 거친 값을 그대로 계승한다.

## Agent skills

### Issue tracker

Issues and specs are tracked as local markdown files under `.scratch/`. See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context layout — `CONTEXT.md` + `docs/adr/` at the repo root. See `docs/agents/domain.md`.
