# CLAUDE.md — Windows IME 상태 인디케이터 (TSF 이벤트 기반)

## 1. 프로젝트 개요

Windows에서 한글/영문 입력 상태(IME)를 화면에 항상 작게 표시해주는 상시 실행 프로그램을 만든다.
사용자는 듀얼 모니터 환경에서 작업하며, 현재 입력 상태(한글/영문)를 매번 인지하지 못해 오타가
반복되는 문제를 겪고 있다. 화면 구석에 상시 표시되는 작은 인디케이터로 이를 해결한다.

## 2. 배경 — 기존 프로토타입(AutoHotkey)에서 확인된 사실

AutoHotkey로 먼저 프로토타입을 만들어 검증했고, 이 과정에서 아래 사실들을 확인했다.
새 구현(C#)은 이 교훈을 반영해야 한다.

- 이 시스템은 모든 애플리케이션에서 레거시 IMM API(`ImmGetContext`)가 `himc=0`을 반환한다.
  즉 이 PC의 입력기는 **TSF(Text Services Framework)로만 동작**하며, 레거시 IMM 호환 계층이
  사실상 작동하지 않는다.
- 차선책으로 `VK_HANGUL`(가상키코드 0x15)의 전역 토글 상태(Caps Lock과 같은 방식)를 읽는
  방식을 시도했으나, 이 값은 TSF가 관리하는 "진짜 상태"의 **메아리(echo)**에 불과했다.
  실제 입력 상태가 바뀌어도 이 토글 값이 갱신되지 않는 경우가 있어, 표시와 실제 상태가
  어긋난 채로 장시간 지속되는 문제가 반복됐다.
- 폴링(주기적 재확인) 방식으로 이 값을 반복 조회하는 것은 근본적인 해결책이 되지 못했다.
  값 자체가 부정확하면 몇 번을 다시 읽어도 같은 부정확한 값만 반환되기 때문이다.
- **결론: 반드시 TSF의 진짜 상태 저장소(Compartment)를 직접 읽어야 하며, 폴링이 아니라
  TSF의 이벤트 콜백(`ITfSource` / `AdviseSink`)을 구독하는 방식으로 구현해야 타이밍 어긋남
  문제가 구조적으로 발생하지 않는다.**

## 3. 목표 / 비목표

**목표**
- 현재 포커스된 컨텍스트의 한/영 입력 상태를 **오차 없이, 실시간으로** 표시
- 폴링 없이 TSF 이벤트 구독만으로 상태 변화 감지
- 여러 모니터 각각에 인디케이터 표시

**비목표**
- IME 자체의 동작을 변경하거나 가로채는 기능 (표시 전용)
- 한글 외 다른 언어(일본어 가나 등) 지원 — 필요 시 추후 확장

**알려진 제약** (실측으로 확인, 원인·해결 여부는 ADR-0005 Update 참고)
- 클래식 콘솔 창(`cmd.exe` 등, `conhost.exe` 기반)은 보이는 창의 PID와 실제 TIP이 로드되는
  프로세스(`conhost.exe`)가 달라 정확히 반영되지 않는다. Windows Terminal·mintty 등 자체
  창을 그리는 터미널에는 해당 없음.
- Excel(및 다른 Office 앱으로 추정)은 셀 편집 중에도 TIP이 로드되지 않아 반영되지 않는다.
- 두 경우 모두 크래시 없이 마지막으로 알려진 상태를 유지할 뿐이라 4절의 방어 규칙은 지켜지지만, "오차 없이" 목표는 이 두 앱 범주에서는 달성되지 않는다.

## 4. 핵심 기술 요구사항

- 언어/런타임: C# (.NET 8) WinForms UI 프로세스 + C++ TIP(Text Input Processor) DLL,
  Windows 전용 (ADR-0001)
- 실제 테스트로 확인된 사실: 이 PC에서는 앱(창)마다 한/영 입력 상태가 **독립적으로** 유지된다
  (ADR-0002).
- TIP DLL을 시스템에 등록해 텍스트 입력을 다루는 거의 모든 프로세스에 로드시킨다
  (ADR-0003). 독립 프로세스 자신의 `ITfThreadMgr`로는 다른 프로세스의 TSF 상태를 볼 수
  없기 때문이다.
- TIP 내부에서 `ITfThreadMgr`을 `GetGlobalCompartment()`가 아니라 직접
  `ITfCompartmentMgr`로 QueryInterface해 얻는 **스레드 스코프** 컴파트먼트로
  `GUID_COMPARTMENT_KEYBOARD_OPENCLOSE`를 구독한다(문서/전역/컨텍스트 스코프는 관찰되지
  않음 — ADR-0005).
- 상태가 바뀌면 `ITfCompartmentEventSink::OnChange`로 통지받아, 그 프로세스의 PID와 함께
  IPC(named pipe)로 UI 프로세스에 보고한다.
- UI 프로세스는 별도로 `SetWinEventHook(EVENT_SYSTEM_FOREGROUND)`로 현재 포커스된
  프로세스의 PID를 추적하고, IPC로 보고받은 PID별 상태 테이블에서 그 PID의 값을 찾아
  표시한다. 포커스 앱과 TIP 인스턴스를 매칭하는 키는 **PID**여야 한다(스레드ID는 최신
  패키지형 앱에서 TIP 활성화 스레드와 창 소유 스레드가 달라 불일치 — ADR-0005).
- **폴링 타이머를 사용하지 않는다.** 상태 갱신은 TSF 콜백(TIP 내부)과 OS 포커스 이벤트
  (UI 프로세스)에 의해서만 트리거된다.
- 각 TIP은 로드 시점(`Activate`)에 최초 1회 컴파트먼트 값을 읽어 보고한다. UI 프로세스도
  시작 시 1회 현재 포그라운드 PID를 읽어 초기값을 채운다(이후로는 콜백/이벤트에만 의존).
- COM 호출 실패, IPC 연결 실패 등에 대한 방어적 처리 포함 — TIP 쪽은 IPC 실패 시 조용히
  무시하고 계속 동작, UI 쪽은 특정 PID의 보고를 아직 못 받았으면 **화면을 건드리지 않고
  마지막으로 알려진 상태를 유지**한다. 마지막 상태 자체가 없는 시작 직후에는 영문 모드
  (파랑/"A")를 기본값으로 시작한다.

## 5. UI 스펙

- 모니터 수를 자동 감지하여, **각 모니터 상단 중앙**에 인디케이터 창을 하나씩 표시
  (모니터 상단에서 2px 여백). 이 감지는 **프로그램 시작 시 1회**로 충분하다 — 실행 중
  모니터 구성 변경(도킹/언도킹 등)에 대한 실시간 재배치는 범위 밖이며, 반영하려면
  재시작하면 된다.
- 창 크기: 정사각형, 한 변 26px (조정 가능하도록 설정값으로 분리 — **빌드 시점 코드
  상수**로 관리하며, 런타임에 읽는 외부 설정 파일은 두지 않는다. single-file 배포 목표와
  트레이 메뉴에 별도 설정 UI가 없다는 점에 부합)
- 배경색: 완전 불투명
  - 한글 모드: 빨강 (`#FF0000`)
  - 영문 모드: 파랑 (`#0000FF`)
- 텍스트: 흰색, 굵게, 18pt, 맑은 고딕(Malgun Gothic), 가운데 정렬
  - 한글 모드: "한"
  - 영문 모드: "A"
- 창 속성: 항상 위(topmost), 포커스를 가져가지 않음(no-activate), 작업표시줄/Alt-Tab
  목록에 표시되지 않음(tool window)
- 리소스 사용량 최소화 (상시 실행 프로그램이므로 CPU/메모리 사용을 낮게 유지)

## 6. 상호작용 (안전장치)

TSF 이벤트 기반이면 이론적으로 어긋남이 발생하지 않아야 하지만, 예외적인 상황(레거시
프로그램, COM 이벤트 유실 등)에 대비한 수동 교정 기능을 남겨둔다.

- 인디케이터를 **클릭**하면 현재 표시를 즉시 반대로 뒤집는다(API 재조회 없이 단순 반전).
- 여러 모니터에 인디케이터가 떠 있는 경우, 하나를 클릭하면 **모든 모니터의 인디케이터가
  함께 반전**된다. 인디케이터는 모니터마다 독립된 상태가 아니라 "현재 포커스된 컨텍스트의
  상태"라는 하나의 논리적 값을 복제해 보여주는 것이므로, 일부만 반전되면 서로 다른 값을
  보여주게 되어 오히려 혼란을 키운다.
- 이 수동 교정 이후에도 실제 TSF 이벤트가 발생하면 정상적으로 새로운 상태를 반영해야 한다.
- 매뉴얼 교정 수단은 **클릭만** 지원한다. AHK 프로토타입에 있던 전역 단축키(`Ctrl+Alt+W`)는
  의도적으로 두지 않는다 — 7번 항목의 "새 전역 단축키를 점유하지 않는다"는 방침과
  일치시키기 위함이다.

## 7. 비기능 요구사항

- 시작프로그램 등록: `HKCU\Software\Microsoft\Windows\CurrentVersion\Run` 레지스트리 키에
  자기 자신의 실행 파일 경로를 등록하는 방식을 사용한다(관리자 권한 불필요, 로그인 시에만
  실행되면 충분). 등록/해제는 트레이 메뉴에서 토글할 수 있어야 한다.
- 배포 형태: WinForms UI EXE는 .NET 8 기준 **self-contained 단일 파일(single-file)**로
  게시한다(`dotnet publish -r win-x64 --self-contained -p:PublishSingleFile=true`). UI
  EXE 자체 실행에는 여전히 .NET 런타임 설치가 불필요하다. 다만 TIP DLL은 별도로 COM/TSF에
  등록하는 1회성 설치 단계가 필요하며, **이 등록은 관리자 권한이 있어야 한다**(HKCU만으로는
  COM 등록 자체는 성공하지만 TSF가 실제로 TIP을 로드하지 않는 것을 실측으로 확인 — ADR-0005
  Update 참고). 설치 시 1회 관리자 권한 상승(UAC)이 필요하다는 뜻이며, 이후 UI EXE의
  평소 실행에는 관리자 권한이 필요 없다.
- 트레이 아이콘: 현재 한/영 상태를 반영하지 않는 고정 아이콘을 사용한다(상태 표시는
  인디케이터 창이 전담). 디자인은 `docs/assets/tray-icon-reference.png`(남색 아웃라인의
  모니터 라인아트 스타일)를 참고하되, 중앙 심볼은 느낌표(`!`) 대신 `A` 글자로 대체한다.
  우클릭 컨텍스트 메뉴는 다음 두 항목으로 구성한다:
  - **시작프로그램 등록/해제** (현재 등록 여부에 따라 토글)
  - **종료**
- 사용자는 이미 별도의 AutoHotkey 스크립트(ESC 키 → 영문 강제 전환)를 시작프로그램에서
  함께 실행 중이다. 이 프로그램은 그 스크립트와 **독립적으로 동시에 실행되어도 충돌이
  없어야 한다** (전역 단축키를 새로 점유하지 않는 것을 권장).

## 8. 참고 — 이전 프로토타입 파일

`ime_indicator.ahk` (AutoHotkey, 참고용 — 이번 프로젝트에서는 사용하지 않음): 위 3번
항목에 정리된 한계로 인해 대체되는 것이며, UI 스펙(색상/폰트/위치 방식)은 이 파일에서
이미 사용자 검증을 거친 값을 그대로 계승한다. 크기·여백은 5절에 적힌 값을 따른다.

`docs/assets/tray-icon-reference.png`: 트레이 아이콘 디자인 참고 이미지. 7번 항목 참고.

## Agent skills

### Issue tracker

Issues and specs are tracked as local markdown files under `.scratch/`. See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context layout — `CONTEXT.md` + `docs/adr/` at the repo root. See `docs/agents/domain.md`.
