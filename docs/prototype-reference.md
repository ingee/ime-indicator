# 참고 자료 — 이전 프로토타입

`ime_indicator.ahk`는 이번 프로젝트에서 실행하지 않는 AHK 프로토타입이다. `CLAUDE.md` 배경
절에서 설명하는 TSF 이벤트 기반 결론은 이 프로토타입을 검증하며 얻은 교훈이며,
[UI 스펙](ui-spec.md)의 화면 표시값(색상·폰트·위치)도 여기서 이미 검증된 값을 그대로
계승한다.

`docs/assets/tray-icon-reference.png`는 트레이 아이콘 디자인 참고 이미지다.
